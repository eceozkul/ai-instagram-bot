name: AI Instagram Bot

on:
  schedule:
    - cron: '0 */4 * * *'  # Her 4 saatte bir: 00:00, 04:00, 08:00, 12:00, 16:00, 20:00 UTC
  workflow_dispatch:

concurrency:
  group: instagram-bot
  cancel-in-progress: false  # Onay bekleyen çalışma varken yenisi kuyruğa girer, üst üste binmez

jobs:
  post:
    runs-on: ubuntu-latest
    permissions:
      contents: write  # Görselleri commit + push için gerekli
    steps:
      - uses: actions/checkout@v5
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - uses: actions/setup-python@v6
        with:
          python-version: '3.11'

      - name: Bağımlılıkları yükle
        run: pip install -r requirements.txt

      # Chromium ~150MB — her çalışmada indirmemek için önbelleklenir
      - name: Playwright Chromium önbelleği
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-${{ runner.os }}-${{ hashFiles('requirements.txt') }}

      - name: Chromium yükle
        run: python -m playwright install --with-deps chromium

      - name: Çalıştır
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          META_ACCESS_TOKEN: ${{ secrets.META_ACCESS_TOKEN }}
          IG_BUSINESS_ID: ${{ secrets.IG_BUSINESS_ID }}
          GITHUB_REPO: ${{ github.repository }}
          GOOGLE_SERVICE_ACCOUNT: ${{ secrets.GOOGLE_SERVICE_ACCOUNT }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          LANGUAGE: tr
        run: |
          cd src
          python main.py
